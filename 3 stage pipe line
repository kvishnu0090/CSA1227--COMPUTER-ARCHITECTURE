#include <stdio.h>

int main() {
    int operandA = 10;
    int operandB = 5;
    int result;

    // Fetch Stage
    result = operandA;

    // Decode Stage
    result = result * operandB;

    // Execute Stage

    printf("Result: %d\n", result);
    printf("Performance: 1 clock cycle\n");

    return 0;
}
